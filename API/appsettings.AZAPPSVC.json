// The settings in this file will be used when running on Azure App Service, unless overridden by an environment-specific file.
{
  // The following values are provided through an Azure Key Vault:
  // - Database:SQLServerConnectionString
  // - Geo:HereMapsAPIKey
  // - AuthNR:JWTSigningKey

  "AuthNR": { // JWTSigningKey should be stored as user-secret.
    "JWTAuthority": "https://fvect-backend.azurewebsites.net/",
    "JWTAudience": "https://fvect-backend.azurewebsites.net/"
  }
}
