// Copy this file to "appsettings.Local.json" to override the current environment settings.
// "appsettings.Local.json" is ignored by version control.

{
  "Database": {
    // This connection string assumes that a local SQL Server is running to which the application identity
    // is allowed admin access.
    "SQLServerConnectionString": "Server=localhost;Database=FVectLocal;Trusted_Connection=True;"
  },
  "AuthNR": { // JWTSigningKey should be stored as user-secret.
    "JWTAuthority": "https://localhost:5001/",
    "JWTAudience": "https://localhost:5001/"
  }
}
